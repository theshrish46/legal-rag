# 1. Python specific (Bulky & unnecessary)
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
.pytest_cache/

# 2. Version Control (Huge history files you don't need in the container)
.git
.gitignore

# 3. Sensitive Info (Very important!)
# We pass keys via 'environment' in Compose, so don't bake them into the image
.env
.env.local

# 4. Local Database/Storage (Prevents local data from bloating the image)
qdrant_storage/
data/
DATA

# 5. Docker files (Docker doesn't need to copy its own instructions)
Dockerfile
docker-compose.yml
.dockerignore

# 6. IDE Settings (Mac/Windows/VS Code junk)
.vscode/
.idea/
.DS_Store